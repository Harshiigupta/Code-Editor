# Python Code Editor with Terminal (Flask + Docker)

## Features
- Python code editor using CodeMirror
- Accepts user input
- Executes in Docker container
- Displays output in terminal-like UI

## Run Locally
```bash
chmod +x run.sh
./run.sh
```